// Stub for marked — peer dep required by glide-data-grid's MarkdownCell, not used here.
export const marked = () => "";
export const Marked = class {};
export default { marked: () => "", parse: () => "" };
